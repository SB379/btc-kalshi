[package]
name = "reconstructor"
version = "0.1.0"
edition = "2021"

[dependencies]
shared-types = { path = "../../shared/types" }
tokio = { version = "1", features = ["full"] }
ringbuf = "0.4"
tracing = "0.1"

historian = { path = "../../data/historian" }
